//cmd.CommandText = "SELECT O.Name, C.Name, C.Length, T.Name FROM SYSOBJECTS O  JOIN SYSCOLUMNS C ON(C.id = O.id) JOIN SYSTYPES T ON(C.xtype = T.xtype) WHERE O.XType = 'U' ORDER BY O.Name";
            cmd.CommandText = "SELECT C.TABLE_NAME AS TName, C.COLUMN_NAME AS CName, C.DATA_TYPE AS DType, C.CHARACTER_MAXIMUM_LENGTH AS DLength, C.IS_NULLABLE AS IsNullable FROM INFORMATION_SCHEMA.COLUMNS C ORDER BY C.TABLE_NAME";
